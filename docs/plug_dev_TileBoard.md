# 磁贴面板 - TileBoard

```bash
# 切换到HA配置目录
cd ~/.homeassistant

# 下载项目
git clone --depth=1 https://github.com.cnpmjs.org/resoai/TileBoard

# 删除git数据
sudo rm -rf TileBoard/.git

# 将TileBoard移动到www目录（如果没有www自己新建）
sudo cp -r TileBoard www/
```